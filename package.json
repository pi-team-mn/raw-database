{
  "name": "@pi-team-mn/raw-database",
  "description": "Read raw JSON files and parse them into an in memory database",
  "author": "pi-team@mn.nl (David Diks <david.diks@mn.nl>)",
  "version": "1.1.0",
  "main": "lib/main.js",
  "types": "lib/main.d.ts",
  "scripts": {
    "test": "mocha --require ts-node/register ./src/**.spec.ts",
    "tsc": "tsc",
    "deploy": "npm publish --access public",
    "publish": "npm run tsc && npm run deploy"
  },
  "files": [
    "lib/**",
    "!lib/**.spec.**"
  ],
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.2.5",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.12.14",
    "chai": "^4.2.0",
    "mocha": "^10.2.0",
    "ts-node": "^8.5.3",
    "typescript": "^3.7.2"
  }
}
